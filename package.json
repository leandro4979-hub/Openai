{
  "name": "nexus-omni-studio",
  "version": "1.0.0",
  "private": true,
  "description": "A focused prompt workspace and reusable AI image generation action.",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check index.js && node scripts/check-project.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^3.0.1"
  }
}
