      - name: Sync Scripts
        env:
          # This tells the script to grab the secrets you just saved
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PROXY_SERVER: ${{ secrets.PROXY_SERVER }}
        run: |
          # ... the rest of the node script ...
